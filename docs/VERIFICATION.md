# Проверки версии 0.3.0 и обновления данных

Дата: 23 сентября 2026. В эту итерацию добавлены недостающие примеры, обязательная ссылка на прототип и проверка URL на сервере.

## Проверено

| Проверка | Результат |
|---|---|
| Синтаксис интерфейса | `node --check app.js` — успешно |
| Компиляция backend | `python3 -m py_compile backend/main.py backend/ai.py backend/scoring.py` — успешно |
| Серверные тесты | `10 passed`; один deprecation warning от связки Starlette TestClient/HTTPX |
| Ссылка на прототип | Пропущенная/пустая и небезопасная ссылка отклоняется; корректная HTTP(S) принимается |
| Демо-набор | 5 карточек задач, 5 профилей команд, 5 предложений; у всех предложений есть ссылка |
| Существующее состояние демо | Недостающие образцы добавляются по ID; пользовательские данные сохраняются |
| Полный серверный сценарий | Два отдельных аккаунта: создание → публикация → отклик со ссылкой → ручной выбор → подтверждение прогресса; успешно в Playwright |
| Автономное демо | Создание задачи → отклик → ручное решение → подтверждение результата — успешно; JS-ошибок нет |
| Телефон | Ширина 390 px; горизонтального переполнения нет |

## Ограничения релиза

- Живой запрос внешней LLM не запускался: нужен API-ключ. Контракт и ошибки проверяются тестовым провайдером.
- Публичное автономное демо доступно: [https://ansaai.netlify.app](https://ansaai.netlify.app). Это статическая сборка; серверные аккаунты и live LLM в ней не подключены.
- Docker Engine отсутствовал; образ не собирался.
- Автономное HTML-демо использует локальные учебные данные. Серверная версия поддерживает отдельные аккаунты и внешний AI.
