# Проверки версии 0.3.0

Дата: 23 сентября 2026. Проверена собранная версия из этого архива.

| Проверка | Результат |
|---|---|
| Синтаксис JavaScript | `node --check app.js` — успешно |
| Компиляция Python | main.py, ai.py, scoring.py — успешно |
| Серверные тесты | **10 passed** |
| Регистрация, вход, выход, CSRF | Проверены |
| Права владельца, роли, скрытие чужих откликов | Проверены |
| Повторная публикация, конфликт версии | Проверены |
| Одновременное подтверждение прогресса | 8 одновременных запросов; итоговые баллы не дублируются |
| Защита ссылок и публичных файлов | javascript: отклоняется; .env, SQLite, backend не раздаются |
| Внешний AI | Контракт проверен тестовым провайдером; живой запрос НЕ запускался |
| Ошибка AI | Возвращается ошибка; скрытой подмены локальным ответом нет |
| Локальный анализ | 10 из 10 кейсов проходят структурные проверки |
| Полный браузерный путь | Отдельные бизнес/команда, задача → отклик → выбор → прогресс; успешно |
| Баллы | Тестовая полная задача 100/100; команда получает 5+10=15 |
| Перезагрузка | Задачи доступны после перезагрузки и входа из нового клиента |
| Автономное демо | Открывается из файла, используется в резервной видеозаписи |
| Телефон | Ширина 390 px, горизонтального переполнения нет |
| Публичный сервер / Docker | Конфигурация подготовлена; публикация и Docker-сборка НЕ выполнялись |

10 локальных кейсов не доказывают качество внешней модели: они проверяют количество вопросов, допустимый режим и структуру. Для качества AI требуется экспертно просмотреть результаты `scripts/evaluate_ai.py --live` с действующим ключом.

Тестовая среда: Python 3.12, FastAPI 0.141.1, Uvicorn 0.53.0, HTTPX 0.28.1, Pytest 9.1.1, Chromium 153. Серверные тесты вывели одно предупреждение deprecation совместимости Starlette TestClient/HTTPX; ошибки отсутствуют.
